export enum DateFromat {
    dayMonthYearHourMinute = "DD MMM YYYY hh:mm",
    dayMonthYear = "DD MMM YYYY", // this type we don't use in this small application, but this is example how we can make more date formats and pass as second argument in convertTimestampToDate function
}